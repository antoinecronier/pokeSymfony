<?php

namespace Pokebattle\ApiBundle\Controller;

use FOS\RestBundle\Controller\Annotations as Rest;
use FOS\RestBundle\Request\ParamFetcherInterface;

use Nelmio\ApiDocBundle\Annotation\ApiDoc;

use Symfony\Component\HttpFoundation\Request;

use Pokebattle\ApiBundle\Form\PokeObjetType;
use Pokebattle\ApiBundle\Entity\PokeObjet;
use Pokebattle\ApiBundle\Controller\RestPokeNpcController;
use Pokebattle\ApiBundle\Controller\RestPokeTypeObjetController;
use Pokebattle\ApiBundle\Controller\RestSyncController as Controller;

/**
 * PokeObjet Repository
 *
 * This class was generated by the Harmony / Doctrine ORM.
 *
 * This class will be regenerated by Harmony. DO NOT MODIFIY THIS CLASS.
 */
class RestPokeObjetControllerBase extends Controller {

    public function getNewEntity() {
        return new PokeObjet();
    }

    /**=========================================================================
     * CRUD REST
     *========================================================================*/

    /**
     * GET /{id}
     *
     * @param string pokeObjetId
     * @return Response
     * @Rest\Get("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeObjet",
     *   description="Show entity"
     * )
     * @return type
     */
    public function getPokeObjetAction($id)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $data = $entityManager->getRepository('PokebattleApiBundle:PokeObjet')->findOneById($id);
        $view = $this->view($data, 200);
        return $this->handleView($view);
    }

    /**
     * POST
     *
     * @Rest\Post("")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeObjet",
     *   description="Create entity",
     *   requirements={
     *      {
     *          "name"="pokeNpcobjetsInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeNpcobjetsInternal pokeObjet",
     *      },
     *      {
     *          "name"="nom",
     *          "dataType"="string",
     *          "requirement"="",
     *          "description"="The nom pokeObjet",
     *      },
     *      {
     *          "name"="quantity",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The quantity pokeObjet",
     *      },
     *      {
     *          "name"="type",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The type pokeObjet",
     *      },
     *   }
     * )
     * @return type
     */
    public function createPokeObjetAction(Request $request) {
        $entity = new PokeObjet();

        $form = $this->createForm(new PokeObjetType(), $entity, array(
            'allow_extra_fields' => true
        ));
        $form->submit($request);

        if ($form->isValid()) {
            $entityManager = $this->getDoctrine()->getManager();
            $entityManager->persist($entity);
            $entityManager->flush();
            $view = $this->view($entity, 200);
        } else {
            $view = $this->view(array("error" => "Parameters entity isn't valid" . $form->getErrors()), 400);
        }

        return $this->handleView($view);
    }

    /**
     * GET
     *
     * @Rest\Get("")
     * @Rest\QueryParam(name="page", nullable=true, requirements="\d+", default="0", description="Number of the page")
     * @Rest\QueryParam(name="quantity", nullable=true, requirements="\d+", default="0", description="Quantity of entities per page")
     * @param ParamFetcher $paramFetcher
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeObjet",
     *   description="Show all entities",
     *   requirements={
     *   }
     * )
     * @return type
     */
    public function getAllPokeObjetAction(ParamFetcherInterface $paramFetcher)
    {
        $repository = $this->getDoctrine()->getRepository('PokebattleApiBundle:PokeObjet');

        $page = $paramFetcher->get('page');
        $quantity = $paramFetcher->get('quantity');

        $data = $repository->getList($page, $quantity);

        if ($data) {
            $view = $this->view(array("PokeObjets" => $data), 200);
        } else {
            $view = $this->view(null, 204);
        }

        return $this->handleView($view);
    }

    /**
     * PUT /{id}
     *
     * @Rest\Put("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeObjet",
     *   description="Update entity",
     *   requirements={
     *      {
     *          "name"="pokeNpcobjetsInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeNpcobjetsInternal pokeObjet",
     *      },
     *      {
     *          "name"="nom",
     *          "dataType"="string",
     *          "requirement"="",
     *          "description"="The nom pokeObjet",
     *      },
     *      {
     *          "name"="quantity",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The quantity pokeObjet",
     *      },
     *      {
     *          "name"="type",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The type pokeObjet",
     *      },
     *   }
     * )
     * @return type
     */
    public function updatePokeObjetAction($id, Request $request)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $entity = $entityManager->getRepository('PokebattleApiBundle:PokeObjet')
            ->findOneById($id);

        $form = $this->createForm(new PokeObjetType(), $entity, array(
            'allow_extra_fields' => true
        ));

        $form->submit($request, false);

        if ($form->isValid()) {
            $entityManager = $this->getDoctrine()->getManager();
            $entityManager->persist($entity);
            $entityManager->flush();
            $view = $this->view($entity, 200);
        } else {
            $view = $this->view(array("error" => "Parameters entity isn't valid"), 400);
        }

        return $this->handleView($view);
    }

    /**
     * DELETE /{id}
     *
     * @Rest\Delete("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeObjet",
     *   description="Delete entity",
     * )
     * @return type
     */
    public function deletePokeObjetAction($id)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $entity = $entityManager->getRepository('PokebattleApiBundle:PokeObjet')
            ->find(array("id" => $id));

        if (!$entity) {
            throw $this->createNotFoundException('Unable to find PokeObjet entity');
        }

        $entityManager->remove($entity);
        $entityManager->flush();

        $view = $this->view($entity, 200);
        return $this->handleView($view);
    }


}

?>