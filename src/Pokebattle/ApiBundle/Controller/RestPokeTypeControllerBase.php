<?php

namespace Pokebattle\ApiBundle\Controller;

use FOS\RestBundle\Controller\Annotations as Rest;
use FOS\RestBundle\Request\ParamFetcherInterface;

use Nelmio\ApiDocBundle\Annotation\ApiDoc;

use Symfony\Component\HttpFoundation\Request;

use Pokebattle\ApiBundle\Form\PokeTypeType;
use Pokebattle\ApiBundle\Entity\PokeType;
use Pokebattle\ApiBundle\Controller\RestPokeTypeController;
use Pokebattle\ApiBundle\Controller\RestPokeTypePokemonController;
use Pokebattle\ApiBundle\Controller\RestSyncController as Controller;

/**
 * PokeType Repository
 *
 * This class was generated by the Harmony / Doctrine ORM.
 *
 * This class will be regenerated by Harmony. DO NOT MODIFIY THIS CLASS.
 */
class RestPokeTypeControllerBase extends Controller {

    public function getNewEntity() {
        return new PokeType();
    }

    /**=========================================================================
     * CRUD REST
     *========================================================================*/

    /**
     * GET /{id}
     *
     * @param string pokeTypeId
     * @return Response
     * @Rest\Get("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeType",
     *   description="Show entity"
     * )
     * @return type
     */
    public function getPokeTypeAction($id)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $data = $entityManager->getRepository('PokebattleApiBundle:PokeType')->findOneById($id);
        $view = $this->view($data, 200);
        return $this->handleView($view);
    }

    /**
     * POST
     *
     * @Rest\Post("")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeType",
     *   description="Create entity",
     *   requirements={
     *      {
     *          "name"="nom",
     *          "dataType"="string",
     *          "requirement"="",
     *          "description"="The nom pokeType",
     *      },
     *      {
     *          "name"="modificateur",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The modificateur pokeType",
     *      },
     *      {
     *          "name"="pokeTypetypeFortInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeTypetypeFortInternal pokeType",
     *      },
     *      {
     *          "name"="typeFort",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The typeFort pokeType",
     *      },
     *      {
     *          "name"="pokeTypetypeFaibleInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeTypetypeFaibleInternal pokeType",
     *      },
     *      {
     *          "name"="typeFaible",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The typeFaible pokeType",
     *      },
     *      {
     *          "name"="pokeTypePokemontypesInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeTypePokemontypesInternal pokeType",
     *      },
     *   }
     * )
     * @return type
     */
    public function createPokeTypeAction(Request $request) {
        $entity = new PokeType();

        $form = $this->createForm(new PokeTypeType(), $entity, array(
            'allow_extra_fields' => true
        ));
        $form->submit($request);

        if ($form->isValid()) {
            $entityManager = $this->getDoctrine()->getManager();
            $entityManager->persist($entity);
            $entityManager->flush();
            $view = $this->view($entity, 200);
        } else {
            $view = $this->view(array("error" => "Parameters entity isn't valid" . $form->getErrors()), 400);
        }

        return $this->handleView($view);
    }

    /**
     * GET
     *
     * @Rest\Get("")
     * @Rest\QueryParam(name="page", nullable=true, requirements="\d+", default="0", description="Number of the page")
     * @Rest\QueryParam(name="quantity", nullable=true, requirements="\d+", default="0", description="Quantity of entities per page")
     * @param ParamFetcher $paramFetcher
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeType",
     *   description="Show all entities",
     *   requirements={
     *   }
     * )
     * @return type
     */
    public function getAllPokeTypeAction(ParamFetcherInterface $paramFetcher)
    {
        $repository = $this->getDoctrine()->getRepository('PokebattleApiBundle:PokeType');

        $page = $paramFetcher->get('page');
        $quantity = $paramFetcher->get('quantity');

        $data = $repository->getList($page, $quantity);

        if ($data) {
            $view = $this->view(array("PokeTypes" => $data), 200);
        } else {
            $view = $this->view(null, 204);
        }

        return $this->handleView($view);
    }

    /**
     * PUT /{id}
     *
     * @Rest\Put("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeType",
     *   description="Update entity",
     *   requirements={
     *      {
     *          "name"="nom",
     *          "dataType"="string",
     *          "requirement"="",
     *          "description"="The nom pokeType",
     *      },
     *      {
     *          "name"="modificateur",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The modificateur pokeType",
     *      },
     *      {
     *          "name"="pokeTypetypeFortInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeTypetypeFortInternal pokeType",
     *      },
     *      {
     *          "name"="typeFort",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The typeFort pokeType",
     *      },
     *      {
     *          "name"="pokeTypetypeFaibleInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeTypetypeFaibleInternal pokeType",
     *      },
     *      {
     *          "name"="typeFaible",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The typeFaible pokeType",
     *      },
     *      {
     *          "name"="pokeTypePokemontypesInternal",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The pokeTypePokemontypesInternal pokeType",
     *      },
     *   }
     * )
     * @return type
     */
    public function updatePokeTypeAction($id, Request $request)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $entity = $entityManager->getRepository('PokebattleApiBundle:PokeType')
            ->findOneById($id);

        $form = $this->createForm(new PokeTypeType(), $entity, array(
            'allow_extra_fields' => true
        ));

        $form->submit($request, false);

        if ($form->isValid()) {
            $entityManager = $this->getDoctrine()->getManager();
            $entityManager->persist($entity);
            $entityManager->flush();
            $view = $this->view($entity, 200);
        } else {
            $view = $this->view(array("error" => "Parameters entity isn't valid"), 400);
        }

        return $this->handleView($view);
    }

    /**
     * DELETE /{id}
     *
     * @Rest\Delete("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD PokeType",
     *   description="Delete entity",
     * )
     * @return type
     */
    public function deletePokeTypeAction($id)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $entity = $entityManager->getRepository('PokebattleApiBundle:PokeType')
            ->find(array("id" => $id));

        if (!$entity) {
            throw $this->createNotFoundException('Unable to find PokeType entity');
        }

        $entityManager->remove($entity);
        $entityManager->flush();

        $view = $this->view($entity, 200);
        return $this->handleView($view);
    }


}

?>